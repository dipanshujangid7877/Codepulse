'use strict';

var React = require('react');
var styled = require('styled-components');
var clsx = require('clsx');
var Box = require('../Box/Box.js');
var useId = require('../hooks/useId.js');
var useRefObjectAsForwardedRef = require('../hooks/useRefObjectAsForwardedRef.js');
var useResponsiveValue = require('../hooks/useResponsiveValue.js');
var useSlots = require('../hooks/useSlots.js');
require('@styled-system/css');
var merge = require('deepmerge');
var environment = require('../utils/environment.js');
var useOverflow = require('../hooks/useOverflow.js');
var warning = require('../utils/warning.js');
var useStickyPaneHeight = require('./useStickyPaneHeight.js');
require('../FeatureFlags/FeatureFlags.js');
var useFeatureFlag = require('../FeatureFlags/useFeatureFlag.js');
require('../FeatureFlags/DefaultFeatureFlags.js');
var PageLayout_module = require('./PageLayout.module.css.js');

function _interopDefault (e) { return e && e.__esModule ? e : { default: e }; }

var React__default = /*#__PURE__*/_interopDefault(React);
var merge__default = /*#__PURE__*/_interopDefault(merge);

function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const REGION_ORDER = {
  header: 0,
  paneStart: 1,
  content: 2,
  paneEnd: 3,
  footer: 4
};
const SPACING_MAP = {
  none: 0,
  condensed: 3,
  normal: [3, null, null, 4]
};
const PageLayoutContext = /*#__PURE__*/React__default.default.createContext({
  padding: 'normal',
  rowGap: 'normal',
  columnGap: 'normal',
  paneRef: {
    current: null
  }
});

// ----------------------------------------------------------------------------
// PageLayout

const CSS_MODULES_FEATURE_FLAG = 'primer_react_css_modules_staff';
const containerWidths = {
  full: '100%',
  medium: '768px',
  large: '1012px',
  xlarge: '1280px'
};

// TODO: refs
const Root = ({
  containerWidth = 'xlarge',
  padding = 'normal',
  rowGap = 'normal',
  columnGap = 'normal',
  children,
  sx = {},
  className,
  style,
  _slotsConfig: slotsConfig
}) => {
  const {
    rootRef,
    enableStickyPane,
    disableStickyPane,
    contentTopRef,
    contentBottomRef,
    stickyPaneHeight
  } = useStickyPaneHeight.useStickyPaneHeight();
  const paneRef = React.useRef(null);
  const enabled = useFeatureFlag.useFeatureFlag(CSS_MODULES_FEATURE_FLAG);
  const [slots, rest] = useSlots.useSlots(children, slotsConfig !== null && slotsConfig !== undefined ? slotsConfig : {
    header: Header,
    footer: Footer
  });
  const stylingProps = enabled ? {
    sx,
    className: clsx.clsx(PageLayout_module.PageLayoutRoot, className)
  } : {
    sx: merge__default.default({
      padding: SPACING_MAP[padding]
    }, sx),
    className
  };
  const wrapperStylingProps = enabled ? {
    className: PageLayout_module.PageLayoutWrapper,
    'data-width': containerWidth
  } : {
    sx: {
      maxWidth: containerWidths[containerWidth],
      marginX: 'auto',
      display: 'flex',
      flexWrap: 'wrap'
    }
  };
  const contentStylingProps = enabled ? {
    className: clsx.clsx(PageLayout_module.PageLayoutContent, className)
  } : {
    sx: {
      display: 'flex',
      flex: '1 1 100%',
      flexWrap: 'wrap',
      maxWidth: '100%'
    }
  };
  return /*#__PURE__*/React__default.default.createElement(PageLayoutContext.Provider, {
    value: {
      padding,
      rowGap,
      columnGap,
      enableStickyPane,
      disableStickyPane,
      contentTopRef,
      contentBottomRef,
      paneRef
    }
  }, /*#__PURE__*/React__default.default.createElement(Box, _extends({
    ref: rootRef,
    style: {
      '--sticky-pane-height': stickyPaneHeight,
      '--spacing': `var(--spacing-${padding})`,
      ...style
    }
  }, stylingProps), /*#__PURE__*/React__default.default.createElement(Box, wrapperStylingProps, slots.header, /*#__PURE__*/React__default.default.createElement(Box, contentStylingProps, rest), slots.footer)));
};
Root.displayName = "Root";
Root.displayName = 'PageLayout';

// ----------------------------------------------------------------------------
// Divider (internal)

const horizontalDividerVariants = {
  none: {
    display: 'none'
  },
  line: {
    display: 'block',
    height: 1,
    backgroundColor: 'border.default'
  },
  filled: {
    display: 'block',
    height: 8,
    backgroundColor: 'canvas.inset',
    boxShadow: theme => `inset 0 -1px 0 0 ${theme.colors.border.default}, inset 0 1px 0 0 ${theme.colors.border.default}`
  }
};
function negateSpacingValue(value) {
  if (Array.isArray(value)) {
    // Not using recursion to avoid deeply nested arrays
    return value.map(v => v === null ? null : -v);
  }
  return value === null ? null : -value;
}
const HorizontalDivider = ({
  variant = 'none',
  sx = {},
  className,
  position,
  style
}) => {
  const {
    padding
  } = React__default.default.useContext(PageLayoutContext);
  const responsiveVariant = useResponsiveValue.useResponsiveValue(variant, 'none');
  const enabled = useFeatureFlag.useFeatureFlag(CSS_MODULES_FEATURE_FLAG);
  const stylingProps = enabled ? {
    sx,
    className: clsx.clsx(PageLayout_module.HorizontalDivider, className),
    'data-variant': responsiveVariant,
    'data-position': position,
    style: {
      '--spacing-divider': `var(--spacing-${padding})`,
      ...style
    }
  } : {
    sx: theme => merge__default.default({
      // Stretch divider to viewport edges on narrow screens
      marginX: negateSpacingValue(SPACING_MAP[padding]),
      ...horizontalDividerVariants[responsiveVariant],
      [`@media screen and (min-width: ${theme.breakpoints[1]})`]: {
        marginX: '0 !important'
      }
    }, sx),
    className,
    style
  };
  return /*#__PURE__*/React__default.default.createElement(Box, stylingProps);
};
HorizontalDivider.displayName = "HorizontalDivider";
const verticalDividerVariants = {
  none: {
    display: 'none'
  },
  line: {
    display: 'block',
    width: 1,
    backgroundColor: 'border.default'
  },
  filled: {
    display: 'block',
    width: 8,
    backgroundColor: 'canvas.inset',
    boxShadow: theme => `inset -1px 0 0 0 ${theme.colors.border.default}, inset 1px 0 0 0 ${theme.colors.border.default}`
  }
};
const DraggingGlobalStyles = styled.createGlobalStyle(["body[data-page-layout-dragging=\"true\"]{cursor:col-resize;}body[data-page-layout-dragging=\"true\"] *{user-select:none;}"]);
const VerticalDivider = ({
  variant = 'none',
  draggable = false,
  onDragStart,
  onDrag,
  onDragEnd,
  onDoubleClick,
  position,
  className,
  style,
  sx = {}
}) => {
  const [isDragging, setIsDragging] = React__default.default.useState(false);
  const [isKeyboardDrag, setIsKeyboardDrag] = React__default.default.useState(false);
  const responsiveVariant = useResponsiveValue.useResponsiveValue(variant, 'none');
  const enabled = useFeatureFlag.useFeatureFlag(CSS_MODULES_FEATURE_FLAG);
  const stableOnDrag = React__default.default.useRef(onDrag);
  const stableOnDragEnd = React__default.default.useRef(onDragEnd);
  const {
    paneRef
  } = React__default.default.useContext(PageLayoutContext);
  const [minWidth, setMinWidth] = React__default.default.useState(0);
  const [maxWidth, setMaxWidth] = React__default.default.useState(0);
  const [currentWidth, setCurrentWidth] = React__default.default.useState(0);
  React__default.default.useEffect(() => {
    if (paneRef.current !== null) {
      const paneStyles = getComputedStyle(paneRef.current);
      const maxPaneWidthDiffPixels = paneStyles.getPropertyValue('--pane-max-width-diff');
      const minWidthPixels = paneStyles.getPropertyValue('--pane-min-width');
      const paneWidth = paneRef.current.getBoundingClientRect().width;
      const maxPaneWidthDiff = Number(maxPaneWidthDiffPixels.split('px')[0]);
      const minPaneWidth = Number(minWidthPixels.split('px')[0]);
      const viewportWidth = window.innerWidth;
      const maxPaneWidth = viewportWidth > maxPaneWidthDiff ? viewportWidth - maxPaneWidthDiff : viewportWidth;
      setMinWidth(minPaneWidth);
      setMaxWidth(maxPaneWidth);
      setCurrentWidth(paneWidth || 0);
    }
  }, [paneRef, isKeyboardDrag, isDragging]);
  React__default.default.useEffect(() => {
    stableOnDrag.current = onDrag;
  }, [onDrag]);
  React__default.default.useEffect(() => {
    stableOnDragEnd.current = onDragEnd;
  }, [onDragEnd]);
  React__default.default.useEffect(() => {
    function handleDrag(event) {
      var _stableOnDrag$current;
      (_stableOnDrag$current = stableOnDrag.current) === null || _stableOnDrag$current === undefined ? undefined : _stableOnDrag$current.call(stableOnDrag, event.movementX, false);
      event.preventDefault();
    }
    function handleDragEnd(event) {
      var _stableOnDragEnd$curr;
      setIsDragging(false);
      (_stableOnDragEnd$curr = stableOnDragEnd.current) === null || _stableOnDragEnd$curr === undefined ? undefined : _stableOnDragEnd$curr.call(stableOnDragEnd);
      event.preventDefault();
    }
    function handleKeyDrag(event) {
      var _stableOnDrag$current2;
      let delta = 0;
      // https://github.com/github/accessibility/issues/5101#issuecomment-1822870655
      if ((event.key === 'ArrowLeft' || event.key === 'ArrowDown') && currentWidth > minWidth) {
        delta = -3;
      } else if ((event.key === 'ArrowRight' || event.key === 'ArrowUp') && currentWidth < maxWidth) {
        delta = 3;
      } else {
        return;
      }
      setCurrentWidth(currentWidth + delta);
      (_stableOnDrag$current2 = stableOnDrag.current) === null || _stableOnDrag$current2 === undefined ? undefined : _stableOnDrag$current2.call(stableOnDrag, delta, true);
      event.preventDefault();
    }
    function handleKeyDragEnd(event) {
      var _stableOnDragEnd$curr2;
      setIsKeyboardDrag(false);
      (_stableOnDragEnd$curr2 = stableOnDragEnd.current) === null || _stableOnDragEnd$curr2 === undefined ? undefined : _stableOnDragEnd$curr2.call(stableOnDragEnd);
      event.preventDefault();
    }
    // TODO: Support touch events
    if (isDragging || isKeyboardDrag) {
      window.addEventListener('mousemove', handleDrag);
      window.addEventListener('keydown', handleKeyDrag);
      window.addEventListener('mouseup', handleDragEnd);
      window.addEventListener('keyup', handleKeyDragEnd);
      const body = document.body;
      body === null || body === undefined ? undefined : body.setAttribute('data-page-layout-dragging', 'true');
    } else {
      window.removeEventListener('mousemove', handleDrag);
      window.removeEventListener('mouseup', handleDragEnd);
      window.removeEventListener('keydown', handleKeyDrag);
      window.removeEventListener('keyup', handleKeyDragEnd);
      const body = document.body;
      body === null || body === undefined ? undefined : body.removeAttribute('data-page-layout-dragging');
    }
    return () => {
      window.removeEventListener('mousemove', handleDrag);
      window.removeEventListener('mouseup', handleDragEnd);
      window.removeEventListener('keydown', handleKeyDrag);
      window.removeEventListener('keyup', handleKeyDragEnd);
      const body = document.body;
      body === null || body === undefined ? undefined : body.removeAttribute('data-page-layout-dragging');
    };
  }, [isDragging, isKeyboardDrag, currentWidth, minWidth, maxWidth]);
  const stylingProps = enabled ? {
    sx,
    className: clsx.clsx(PageLayout_module.VerticalDivider, className),
    'data-variant': responsiveVariant,
    'data-position': position,
    style
  } : {
    sx: merge__default.default({
      height: '100%',
      position: 'relative',
      ...verticalDividerVariants[responsiveVariant]
    }, sx),
    className,
    style
  };
  return /*#__PURE__*/React__default.default.createElement(Box, stylingProps, draggable ?
  /*#__PURE__*/
  // Drag handle
  React__default.default.createElement(React__default.default.Fragment, null, /*#__PURE__*/React__default.default.createElement(Box, {
    sx: {
      position: 'absolute',
      inset: '0 -2px',
      cursor: 'col-resize',
      bg: isDragging || isKeyboardDrag ? 'accent.fg' : 'transparent',
      transitionDelay: '0.1s',
      '&:hover': {
        bg: isDragging || isKeyboardDrag ? 'accent.fg' : 'neutral.muted'
      }
    },
    role: "slider",
    "aria-label": "Draggable pane splitter",
    "aria-valuemin": minWidth,
    "aria-valuemax": maxWidth,
    "aria-valuenow": currentWidth,
    "aria-valuetext": `Pane width ${currentWidth} pixels`,
    tabIndex: 0,
    onMouseDown: event => {
      if (event.button === 0) {
        setIsDragging(true);
        onDragStart === null || onDragStart === undefined ? undefined : onDragStart();
      }
    },
    onKeyDown: event => {
      if (event.key === 'ArrowLeft' || event.key === 'ArrowRight' || event.key === 'ArrowUp' || event.key === 'ArrowDown') {
        setIsKeyboardDrag(true);
        onDragStart === null || onDragStart === undefined ? undefined : onDragStart();
      }
    },
    onDoubleClick: onDoubleClick
  }), !enabled && /*#__PURE__*/React__default.default.createElement(DraggingGlobalStyles, null)) : null);
};
VerticalDivider.displayName = "VerticalDivider"; // ----------------------------------------------------------------------------
// PageLayout.Header
const Header = ({
  'aria-label': label,
  'aria-labelledby': labelledBy,
  padding = 'none',
  divider = 'none',
  dividerWhenNarrow = 'inherit',
  hidden = false,
  children,
  style,
  sx = {},
  className
}) => {
  // Combine divider and dividerWhenNarrow for backwards compatibility
  const dividerProp = !useResponsiveValue.isResponsiveValue(divider) && dividerWhenNarrow !== 'inherit' ? {
    regular: divider,
    narrow: dividerWhenNarrow
  } : divider;
  const dividerVariant = useResponsiveValue.useResponsiveValue(dividerProp, 'none');
  const isHidden = useResponsiveValue.useResponsiveValue(hidden, false);
  const {
    rowGap
  } = React__default.default.useContext(PageLayoutContext);
  const enabled = useFeatureFlag.useFeatureFlag(CSS_MODULES_FEATURE_FLAG);
  const headerStylingProps = enabled ? {
    sx,
    className: clsx.clsx(PageLayout_module.Header, className),
    style: {
      '--spacing': `var(--spacing-${rowGap})`
    }
  } : {
    sx: merge__default.default({
      width: '100%',
      marginBottom: SPACING_MAP[rowGap]
    }, sx),
    className
  };
  const contentStylingProps = enabled ? {
    className: PageLayout_module.HeaderContent,
    style: {
      '--spacing': `var(--spacing-${padding})`
    }
  } : {
    sx: {
      padding: SPACING_MAP[padding]
    }
  };
  const dividerStylingProps = enabled ? {
    className: PageLayout_module.HeaderHorizontalDivider,
    style: {
      '--spacing': `var(--spacing-${rowGap})`
    }
  } : {
    sx: {
      marginTop: SPACING_MAP[rowGap]
    }
  };
  return /*#__PURE__*/React__default.default.createElement(Box, _extends({
    as: "header",
    "aria-label": label,
    "aria-labelledby": labelledBy,
    hidden: isHidden,
    style: style
  }, headerStylingProps), /*#__PURE__*/React__default.default.createElement(Box, contentStylingProps, children), /*#__PURE__*/React__default.default.createElement(HorizontalDivider, _extends({
    variant: dividerVariant
  }, dividerStylingProps)));
};
Header.displayName = "Header";
Header.displayName = 'PageLayout.Header';

// ----------------------------------------------------------------------------
// PageLayout.Content

// TODO: Account for pane width when centering content
const contentWidths = {
  full: '100%',
  medium: '768px',
  large: '1012px',
  xlarge: '1280px'
};
const Content = ({
  as = 'main',
  'aria-label': label,
  'aria-labelledby': labelledBy,
  width = 'full',
  padding = 'none',
  hidden = false,
  children,
  sx = {},
  className,
  style
}) => {
  const isHidden = useResponsiveValue.useResponsiveValue(hidden, false);
  const {
    contentTopRef,
    contentBottomRef
  } = React__default.default.useContext(PageLayoutContext);
  const enabled = useFeatureFlag.useFeatureFlag(CSS_MODULES_FEATURE_FLAG);
  const wrapperStylingProps = enabled ? {
    sx,
    className: clsx.clsx(PageLayout_module.ContentWrapper, className),
    'data-is-hidden': isHidden
  } : {
    sx: merge__default.default({
      display: isHidden ? 'none' : 'flex',
      flexDirection: 'column',
      order: REGION_ORDER.content,
      // Set flex-basis to 0% to allow flex-grow to control the width of the content region.
      // Without this, the content region could wrap onto a different line
      // than the pane region on wide viewports if its contents are too wide.
      flexBasis: 0,
      flexGrow: 1,
      flexShrink: 1,
      minWidth: 1 // Hack to prevent overflowing content from pushing the pane region to the next line
    }, sx),
    className
  };
  const stylingProps = enabled ? {
    className: PageLayout_module.Content,
    'data-width': width,
    style: {
      '--spacing': `var(--spacing-${padding})`
    }
  } : {
    sx: {
      width: '100%',
      maxWidth: contentWidths[width],
      marginX: 'auto',
      flexGrow: 1,
      padding: SPACING_MAP[padding]
    }
  };
  return /*#__PURE__*/React__default.default.createElement(Box, _extends({
    as: as,
    "aria-label": label,
    "aria-labelledby": labelledBy,
    style: style
  }, wrapperStylingProps), /*#__PURE__*/React__default.default.createElement(Box, {
    ref: contentTopRef
  }), /*#__PURE__*/React__default.default.createElement(Box, stylingProps, children), /*#__PURE__*/React__default.default.createElement(Box, {
    ref: contentBottomRef
  }));
};
Content.displayName = "Content";
Content.displayName = 'PageLayout.Content';

// ----------------------------------------------------------------------------
// PageLayout.Pane

const isCustomWidthOptions = width => {
  // eslint-disable-next-line @typescript-eslint/no-unnecessary-condition
  return width.default !== undefined;
};
const isPaneWidth = width => {
  return ['small', 'medium', 'large'].includes(width);
};
const panePositions = {
  start: REGION_ORDER.paneStart,
  end: REGION_ORDER.paneEnd
};
const paneWidths = {
  small: ['100%', null, '240px', '256px'],
  medium: ['100%', null, '256px', '296px'],
  large: ['100%', null, '256px', '320px']
};
const defaultPaneWidth = {
  small: 256,
  medium: 296,
  large: 320
};
const overflowProps = {
  tabIndex: 0,
  role: 'region'
};
const Pane = /*#__PURE__*/React__default.default.forwardRef(({
  'aria-label': label,
  'aria-labelledby': labelledBy,
  position: responsivePosition = 'end',
  positionWhenNarrow = 'inherit',
  width = 'medium',
  minWidth = 256,
  padding = 'none',
  resizable = false,
  widthStorageKey = 'paneWidth',
  divider: responsiveDivider = 'none',
  dividerWhenNarrow = 'inherit',
  sticky = false,
  offsetHeader = 0,
  hidden: responsiveHidden = false,
  children,
  id,
  sx = {},
  className,
  style
}, forwardRef) => {
  const enabled = useFeatureFlag.useFeatureFlag(CSS_MODULES_FEATURE_FLAG);

  // Combine position and positionWhenNarrow for backwards compatibility
  const positionProp = !useResponsiveValue.isResponsiveValue(responsivePosition) && positionWhenNarrow !== 'inherit' ? {
    regular: responsivePosition,
    narrow: positionWhenNarrow
  } : responsivePosition;
  const position = useResponsiveValue.useResponsiveValue(positionProp, 'end');

  // Combine divider and dividerWhenNarrow for backwards compatibility
  const dividerProp = !useResponsiveValue.isResponsiveValue(responsiveDivider) && dividerWhenNarrow !== 'inherit' ? {
    regular: responsiveDivider,
    narrow: dividerWhenNarrow
  } : responsiveDivider;
  const dividerVariant = useResponsiveValue.useResponsiveValue(dividerProp, 'none');
  const isHidden = useResponsiveValue.useResponsiveValue(responsiveHidden, false);
  const {
    rowGap,
    columnGap,
    enableStickyPane,
    disableStickyPane,
    paneRef
  } = React__default.default.useContext(PageLayoutContext);
  React__default.default.useEffect(() => {
    if (sticky) {
      enableStickyPane === null || enableStickyPane === undefined ? undefined : enableStickyPane(offsetHeader);
    } else {
      disableStickyPane === null || disableStickyPane === undefined ? undefined : disableStickyPane();
    }
  }, [sticky, enableStickyPane, disableStickyPane, offsetHeader]);
  const getDefaultPaneWidth = width => {
    if (isPaneWidth(width)) {
      return defaultPaneWidth[width];
    } else if (isCustomWidthOptions(width)) {
      return Number(width.default.split('px')[0]);
    }
    return 0;
  };
  const [paneWidth, setPaneWidth] = React__default.default.useState(() => {
    if (!environment.canUseDOM) {
      return getDefaultPaneWidth(width);
    }
    let storedWidth;
    try {
      storedWidth = localStorage.getItem(widthStorageKey);
    } catch (error) {
      storedWidth = null;
    }
    return storedWidth && !isNaN(Number(storedWidth)) ? Number(storedWidth) : getDefaultPaneWidth(width);
  });
  const updatePaneWidth = width => {
    setPaneWidth(width);
    try {
      localStorage.setItem(widthStorageKey, width.toString());
    } catch (error) {
      // Ignore errors
    }
  };
  useRefObjectAsForwardedRef.useRefObjectAsForwardedRef(forwardRef, paneRef);
  const hasOverflow = useOverflow.useOverflow(paneRef);
  const paneId = useId.useId(id);
  const labelProp = {};
  if (hasOverflow) {
    process.env.NODE_ENV !== "production" ? warning.warning(label === undefined && labelledBy === undefined, 'The <PageLayout.Pane> has overflow and `aria-label` or `aria-labelledby` has not been set. ' + 'Please provide `aria-label` or `aria-labelledby` to <PageLayout.Pane> in order to label this ' + 'region.') : undefined;
    if (labelledBy) {
      labelProp['aria-labelledby'] = labelledBy;
    } else if (label) {
      labelProp['aria-label'] = label;
    }
  }
  const paneWrapperStylingProps = enabled ? {
    sx,
    className: clsx.clsx(PageLayout_module.PaneWrapper, className),
    style: {
      '--offset-header': typeof offsetHeader === 'number' ? `${offsetHeader}px` : offsetHeader,
      '--spacing-row': `var(--spacing-${rowGap})`,
      '--spacing-column': `var(--spacing-${columnGap})`,
      ...style
    },
    'data-is-hidden': isHidden,
    'data-position': position,
    'data-sticky': sticky || undefined
  } : {
    className,
    sx: theme => merge__default.default({
      // Narrow viewports
      display: isHidden ? 'none' : 'flex',
      order: panePositions[position],
      width: '100%',
      marginX: 0,
      ...(position === 'end' ? {
        flexDirection: 'column',
        marginTop: SPACING_MAP[rowGap]
      } : {
        flexDirection: 'column-reverse',
        marginBottom: SPACING_MAP[rowGap]
      }),
      // Regular and wide viewports
      [`@media screen and (min-width: ${theme.breakpoints[1]})`]: {
        width: 'auto',
        marginY: '0 !important',
        ...(sticky ? {
          position: 'sticky',
          // If offsetHeader has value, it will stick the pane to the position where the sticky top ends
          // else top will be 0 as the default value of offsetHeader
          top: typeof offsetHeader === 'number' ? `${offsetHeader}px` : offsetHeader,
          maxHeight: 'var(--sticky-pane-height)'
        } : {}),
        ...(position === 'end' ? {
          flexDirection: 'row-reverse',
          marginLeft: SPACING_MAP[columnGap]
        } : {
          flexDirection: 'row',
          marginRight: SPACING_MAP[columnGap]
        })
      }
    }, sx),
    style
  };
  const horizontalDividerStylingProps = enabled ? {
    className: PageLayout_module.PaneHorizontalDivider,
    style: {
      '--spacing': `var(--spacing-${rowGap})`
    }
  } : {
    sx: {
      [position === 'end' ? 'marginBottom' : 'marginTop']: SPACING_MAP[rowGap]
    }
  };
  const verticalDividerStylingProps = enabled ? {
    className: PageLayout_module.PaneVerticalDivider,
    style: {
      '--spacing': `var(--spacing-${columnGap})`
    }
  } : {
    sx: {
      [position === 'end' ? 'marginRight' : 'marginLeft']: SPACING_MAP[columnGap]
    }
  };
  const paneStylingProps = enabled ? {
    className: PageLayout_module.Pane,
    'data-resizable': resizable || undefined,
    style: {
      '--spacing': `var(--spacing-${padding})`,
      '--pane-min-width': isCustomWidthOptions(width) ? width.min : `${minWidth}px`,
      '--pane-max-width': isCustomWidthOptions(width) ? width.max : `calc(100vw - var(--pane-max-width-diff))`,
      '--pane-width-custom': isCustomWidthOptions(width) ? width.default : undefined,
      '--pane-width-size': `var(--pane-width-${isPaneWidth(width) ? width : 'custom'})`,
      '--pane-width': `${paneWidth}px`
    }
  } : {
    sx: theme => ({
      '--pane-min-width': isCustomWidthOptions(width) ? width.min : `${minWidth}px`,
      '--pane-max-width-diff': '511px',
      '--pane-max-width': isCustomWidthOptions(width) ? width.max : `calc(100vw - var(--pane-max-width-diff))`,
      width: resizable ? ['100%', null, `clamp(var(--pane-min-width), var(--pane-width), var(--pane-max-width))`] : isPaneWidth(width) ? paneWidths[width] : width.default,
      padding: SPACING_MAP[padding],
      overflow: [null, null, 'auto'],
      [`@media screen and (min-width: ${theme.breakpoints[3]})`]: {
        '--pane-max-width-diff': '959px'
      }
    }),
    style: {
      '--pane-width': `${paneWidth}px`
    }
  };
  return /*#__PURE__*/React__default.default.createElement(Box, paneWrapperStylingProps, /*#__PURE__*/React__default.default.createElement(HorizontalDivider, _extends({
    variant: {
      narrow: dividerVariant,
      regular: 'none'
    }
  }, horizontalDividerStylingProps, {
    position: position
  })), /*#__PURE__*/React__default.default.createElement(Box, _extends({
    ref: paneRef
  }, hasOverflow ? overflowProps : {}, labelProp, id && {
    id: paneId
  }, paneStylingProps), children), /*#__PURE__*/React__default.default.createElement(VerticalDivider, _extends({
    variant: {
      narrow: 'none',
      // If pane is resizable, always show a vertical divider on regular viewports
      regular: resizable ? 'line' : dividerVariant
    }
    // If pane is resizable, the divider should be draggable
    ,
    draggable: resizable,
    onDrag: (delta, isKeyboard = false) => {
      // Get the number of pixels the divider was dragged
      let deltaWithDirection;
      if (isKeyboard) {
        deltaWithDirection = delta;
      } else {
        deltaWithDirection = position === 'end' ? -delta : delta;
      }
      updatePaneWidth(paneWidth + deltaWithDirection);
    }
    // Ensure `paneWidth` state and actual pane width are in sync when the drag ends
    ,
    onDragEnd: () => {
      var _paneRef$current;
      const paneRect = (_paneRef$current = paneRef.current) === null || _paneRef$current === undefined ? undefined : _paneRef$current.getBoundingClientRect();
      if (!paneRect) return;
      updatePaneWidth(paneRect.width);
    },
    position: position
    // Reset pane width on double click
    ,
    onDoubleClick: () => updatePaneWidth(getDefaultPaneWidth(width))
  }, verticalDividerStylingProps)));
});
Pane.displayName = 'PageLayout.Pane';

// ----------------------------------------------------------------------------
// PageLayout.Footer

const Footer = ({
  'aria-label': label,
  'aria-labelledby': labelledBy,
  padding = 'none',
  divider = 'none',
  dividerWhenNarrow = 'inherit',
  hidden = false,
  children,
  sx = {},
  className,
  style
}) => {
  // Combine divider and dividerWhenNarrow for backwards compatibility
  const dividerProp = !useResponsiveValue.isResponsiveValue(divider) && dividerWhenNarrow !== 'inherit' ? {
    regular: divider,
    narrow: dividerWhenNarrow
  } : divider;
  const dividerVariant = useResponsiveValue.useResponsiveValue(dividerProp, 'none');
  const isHidden = useResponsiveValue.useResponsiveValue(hidden, false);
  const {
    rowGap
  } = React__default.default.useContext(PageLayoutContext);
  const enabled = useFeatureFlag.useFeatureFlag(CSS_MODULES_FEATURE_FLAG);
  const footerStylingProps = enabled ? {
    className: clsx.clsx(PageLayout_module.FooterWrapper, className),
    sx,
    style: {
      '--spacing': `var(--spacing-${rowGap})`,
      ...style
    }
  } : {
    className,
    sx: merge__default.default({
      order: REGION_ORDER.footer,
      width: '100%',
      marginTop: SPACING_MAP[rowGap]
    }, sx),
    style
  };
  const dividerStylingProps = enabled ? {
    className: PageLayout_module.FooterHorizontalDivider,
    style: {
      '--spacing': `var(--spacing-${rowGap})`
    }
  } : {
    sx: {
      marginBottom: SPACING_MAP[rowGap]
    }
  };
  const contentStylingProps = enabled ? {
    className: PageLayout_module.FooterContent,
    style: {
      '--spacing': `var(--spacing-${padding})`
    }
  } : {
    sx: {
      padding: SPACING_MAP[padding]
    }
  };
  return /*#__PURE__*/React__default.default.createElement(Box, _extends({
    as: "footer",
    "aria-label": label,
    "aria-labelledby": labelledBy,
    hidden: isHidden
  }, footerStylingProps), /*#__PURE__*/React__default.default.createElement(HorizontalDivider, _extends({}, dividerStylingProps, {
    variant: dividerVariant
  })), /*#__PURE__*/React__default.default.createElement(Box, contentStylingProps, children));
};
Footer.displayName = "Footer";
Footer.displayName = 'PageLayout.Footer';

// ----------------------------------------------------------------------------
// Export

const PageLayout = Object.assign(Root, {
  Header,
  Content,
  Pane,
  Footer
});

exports.PageLayout = PageLayout;
